name: Weekly Desktop Release

on:
  schedule:
    # Every Sunday at 22:00 UTC
    - cron: '0 22 * * 0'
  workflow_dispatch:
    inputs:
      force:
        description: 'Force build even without changes'
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  check-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Find latest desktop tag
        id: latest_tag
        run: |
          # versionsort.suffix=- sorts prereleases below their release (1.3.0-beta.5 < 1.3.0)
          LATEST=$(git -c versionsort.suffix=- tag -l 'desktop-v*' --sort=-v:refname | head -n1)
          STABLE=$(git tag -l 'desktop-v*' | grep -E '^desktop-v[0-9]+\.[0-9]+\.[0-9]+$' | sort -V | tail -n1)
          echo "latest=${LATEST:-none}" >> $GITHUB_OUTPUT
          echo "tag=${STABLE:-none}" >> $GITHUB_OUTPUT
          echo "Latest tag: ${LATEST:-none}, latest stable: ${STABLE:-none}"

      - name: Check for relevant changes
        id: changes
        run: |
          TAG="${{ steps.latest_tag.outputs.tag }}"
          LATEST="${{ steps.latest_tag.outputs.latest }}"
          FORCE="${{ github.event.inputs.force }}"

          if [[ "$FORCE" == "true" ]]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "Forced build requested"
            exit 0
          fi

          if [[ "${LATEST#desktop-v}" == *-* ]]; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "Prerelease cycle in progress ($LATEST), skipping weekly release"
            exit 0
          fi

          if [[ "$TAG" == "none" ]]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "No previous tag found, building"
            exit 0
          fi

          # Check if web, packages, or desktop source changed
          CHANGED=$(git diff --name-only "$TAG"..HEAD -- \
            apps/web/ \
            apps/desktop/ \
            packages/shared/ \
            packages/chat/ \
            packages/docs/ \
          )

          if [[ -n "$CHANGED" ]]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "Changes found since $TAG:"
            echo "$CHANGED" | head -20
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "No relevant changes since $TAG"
          fi

      - name: Bump version and create tag
        if: steps.changes.outputs.has_changes == 'true'
        run: |
          TAG="${{ steps.latest_tag.outputs.tag }}"

          if [[ "$TAG" == "none" ]]; then
            NEW_VERSION="1.1.1"
          else
            # Extract version, bump patch
            VERSION="${TAG#desktop-v}"
            IFS='.' read -r MAJOR MINOR PATCH <<< "$VERSION"
            PATCH=$((PATCH + 1))
            NEW_VERSION="${MAJOR}.${MINOR}.${PATCH}"
          fi

          NEW_TAG="desktop-v${NEW_VERSION}"
          echo "Creating tag: $NEW_TAG"

          # Update version in tauri.conf.json and package.json
          sed -i "s/\"version\": \".*\"/\"version\": \"${NEW_VERSION}\"/" apps/desktop/src-tauri/tauri.conf.json
          sed -i "0,/\"version\": \".*\"/s/\"version\": \".*\"/\"version\": \"${NEW_VERSION}\"/" apps/desktop/package.json

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add apps/desktop/src-tauri/tauri.conf.json apps/desktop/package.json
          git commit -m "chore(desktop): bump version to ${NEW_VERSION}"
          git tag "$NEW_TAG"
          # master is protected (PR-only); the bump commit lives on the tag, which is what the release build checks out
          git push origin "$NEW_TAG"
