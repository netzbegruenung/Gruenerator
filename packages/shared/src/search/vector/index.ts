// Moved to @gruenerator/query so the MCP server can build without all of shared.
// Re-exported here to keep `@gruenerator/shared/search/vector` consumers working.
export * from '@gruenerator/query/vector';
