// Moved to @gruenerator/query so the MCP server can build without all of shared.
// Re-exported here to keep `@gruenerator/shared/utils` (and internal siblings
// like stringDistance.ts) working unchanged.
export * from '@gruenerator/query/text';
