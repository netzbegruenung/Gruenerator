export {
  TAG_DICTIONARY,
  PLATFORM_DICTIONARY,
  COMBINED_DICTIONARY,
  DIMENSION_TAG_MAP,
} from './tagConstants';
export { useTagAutocomplete } from './useTagAutocomplete';
// Re-export the new unified hook for convenience
export { useTextAutocomplete } from '../../../hooks/useTextAutocomplete';
