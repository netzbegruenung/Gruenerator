import { NotebooksIndexContent } from '../../notebook/components/NotebooksIndexPage';

// "Wissen": the notebook page (chat surface + notebook gallery), sized by the
// shell's full-height flex chain.
const WissenTab = () => <NotebooksIndexContent />;

export default WissenTab;
