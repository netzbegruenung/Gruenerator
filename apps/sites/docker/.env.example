# Gruenerator Sites - Docker Configuration
# Copy this file to .env and configure your settings

# Required: URL of your existing Gruenerator API backend
# This is where /api/* and /auth/* requests will be proxied
API_BASE_URL=https://your-api.example.com

# Optional: Port for local development (Coolify sets this automatically)
# Default: 3000
PORT=3000
