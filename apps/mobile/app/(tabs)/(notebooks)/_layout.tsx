// Notebooks tab is disabled — Chat tab replaces it.
// Keeping files for potential future reintegration.
// Tab is hidden via href: null in ClassicTabLayout / hidden in NativeTabLayout.
import { View } from 'react-native';

export default function NotebooksLayout() {
  return <View />;
}
